project_name:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

project_number:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

budget_amount:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

tenderee:
    - '%s[^ψ：:。，,、”“"联]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

bidding_agency:
    - '%s[^ψ：:。，,、”“"联电]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

liaison:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ。，,]+?)ψ'

contact_information:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ。，,]+?)ψ'

successful_bidde:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

bid_amount:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

tenderopen_time:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

project_leader:
    - '%s[^ψ：:。，,、”“"具]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

project_contact_information:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

agent_contact:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'

bidding_contact:
    - '%s[^ψ：:。，,、”“"]*?[: ：]+?\s*?[ψ]*?([^ψ]+?)ψ'
